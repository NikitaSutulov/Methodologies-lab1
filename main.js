import { solveQuadEqu } from './src/solveQuadEqu.js';

// Temporary test cases
// will be removed after adding at least one input mode
solveQuadEqu(2, -1, 3);
solveQuadEqu(2, 4, 2);
solveQuadEqu(1, 0, 9);

console.log();